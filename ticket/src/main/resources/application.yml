# DataSource Config
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/film
    username: root
    password: moondasscry

server:
  port: 8081

mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true

op-patterns: com.example.ticket.service.*